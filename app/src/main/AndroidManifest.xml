<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name="com.enterprise.app.EnterpriseApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.Enterprise">

        <activity
            android:name="com.enterprise.app.ui.MainActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize">

            <!-- Standard launcher -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!--
                Deep link: enterprise://home
                enterprise://detail/{itemId}/{itemTitle}
                enterprise://search
                enterprise://profile
                enterprise://settings
                enterprise://profile/edit/{userId}

                Nav3 handles deep link routing automatically when
                navDeepLink { uriPattern = ... } is declared in composable().
            -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="http" />
                <data android:scheme="https" />
                <data android:scheme="enterprise" />
                <data android:host="*" />
            </intent-filter>
        </activity>
    </application>
</manifest>