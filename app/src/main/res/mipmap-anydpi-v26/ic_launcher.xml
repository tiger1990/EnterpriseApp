<?xml version="1.0" encoding="utf-8"?>
<adaptive-icon xmlns:android="http://schemas.android.com/apk/res/android">
    <background android:drawable="@color/ic_launcher_background"/>
    <foreground android:drawable="@mipmap/ic_launcher_foreground"/>

    <!--
    NEW: dedicated flat monochrome layer.
    The OS tints this with the wallpaper-derived color when the user
    enables "Themed icons". It is ONLY used for that feature — it does
    NOT affect the regular launcher icon appearance.
-->
    <monochrome android:drawable="@drawable/ic_launcher_monochrome" />
</adaptive-icon>